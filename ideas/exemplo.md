{
  "nome": "Exemplo de ideia",
  "tags": ["exemplo", "markdown"],
  "status": "ideia"
}

# Exemplo de ideia

Este é o conteúdo normal do Markdown.

## Como funciona

O site lê automaticamente todos os arquivos `.md` da pasta `ideas/`.

- O JSON inicial vira metadado.
- O restante do arquivo é renderizado como Markdown.
- Não existe índice para atualizar manualmente.

```python
print("Markdown normal")
```
